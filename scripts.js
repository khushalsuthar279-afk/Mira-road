function toggleMenu() {
    const menu = document.getElementById("menu");
    menu.style.display = menu.style.display === "block" ? "none" : "block";
}

const student = {
  fullName: "rahul",
  age:18,
  cgpa: 22,
};

console.log(student);